# Simple guessing game, where the user is espected to guess the number
# generated by the program.

import random    # standard Python module containing functions that can generate random numbers

# Generate random number between 1 and 10 using the "randint()" function in "random"
randomNum = random.randint(1, 10)

# Get input from user and convert it to int
inputNum = int(input("Guess a number: "))

# Compare number user entered to that generate
# User wins if they match
if inputNum == randomNum:
    print("Success! You win!!")
if inputNum > randomNum:
    print("Too high! try again...")
if inputNum < randomNum:
    print("Too low! try again...")
